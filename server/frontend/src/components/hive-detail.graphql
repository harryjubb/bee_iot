query HiveDetail($hiveSlug: String) {
  hive(hiveSlug: $hiveSlug) {
    id
    uid
    name
    slug
    streamActive
    streamKey
    streamUrl
    sponsor {
      id
      name
      url
      logo
      sponsorshipLevel
    }
  }
}
