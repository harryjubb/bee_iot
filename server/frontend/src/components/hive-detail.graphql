query HiveDetail($hiveUrlName: String) {
  hive(hiveUrlName: $hiveUrlName) {
    id
    uid
    name
    urlName
    streamKey
    streamUrl
    sponsor {
      id
      name
      logo
      sponsorshipLevel
    }
  }
}
