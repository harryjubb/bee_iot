query HiveDetail($hiveSlug: String) {
  hive(hiveSlug: $hiveSlug) {
    id
    uid
    name
    slug
    streamActive
    streamKey
    streamUrl
    dashboardActive
    dashboardUrl
    sponsor {
      id
      name
      url
      logo
      sponsorshipLevel
    }
  }
}
